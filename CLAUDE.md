# 사찰 넷제로 진단 도구 — 작업 인계

불교탄소중립실천단 지수화풍 · 2026. 9. 12.

이 문서는 이 폴더에서 작업할 때 먼저 읽어야 할 배경입니다.
코드만 봐서는 알 수 없는 **왜 그렇게 했는지**를 적었습니다.

---

## 1. 무엇을 왜 만드는가

불교탄소중립실천단 **지수화풍** 모임이 사찰 탄소중립 컨설팅에 쓸 도구입니다.
개인용이 아니라 모임에서 함께 씁니다. 올해 목표는 50개 사찰 로드맵 수립입니다.

지금까지는 사찰 한 곳당 엑셀 시트를 복사해 손으로 계산했습니다.
반나절 넘게 걸렸고, 실제로 계산 오류도 생겼습니다(6장 참조).

**대상은 이미 조사가 끝난 다섯 곳이 아니라 새 사찰입니다.**
새 사찰은 주지스님 인터뷰도, 고지서도, 아무 자료도 없는 데서 시작합니다.

---

## 2. 누가 쓰는가 — 설계 제약

모임의 **시니어 회원**들이 직접 씁니다. 여기서 대부분의 설계 결정이 나왔습니다.

| 원칙 | 뜻 |
|---|---|
| 판단을 요구하지 않는다 | "고압인가요"가 아니라 "고지서에 이 글자가 있나요" |
| 화면이 저 혼자 움직이지 않는다 | 실시간 자동완성 금지. 넣고 버튼을 눌러야 검색 |
| 실패에 다음 행동이 붙어 있다 | 오류 메시지로 끝내지 말 것. 시니어에게 실패는 곧 중단 |
| 명령어를 치게 하지 않는다 | 배치 파일 더블클릭으로 끝나야 함 |
| 모르는 것은 모른다고 표시한다 | 빈칸이 아니라 "현장 확인 필요"로 |

---

## 3. 전체 구조 — 3단계

```
① 방문 전 (사무실·웹)  →  ② 현장 (종이·사진)  →  ③ 방문 후 (사무실·웹)
   주소만 입력              종이 체크리스트          사진 보고 입력
   공개자료로 개략 진단      사진 12장 촬영           정밀 진단 계산
   방문 꾸러미 출력          스님 이야기 듣기         보고서 발행
                            웹 사용 안 함
```

**현장에서는 웹을 쓰지 않습니다.** 산중 사찰은 통신이 불안하고, 스님 앞에서
기기를 조작하는 것도 부담입니다. 사진과 종이만 가져와 사무실에서 입력합니다.

### 지금 만든 범위

**① 방문 전 개략 진단**만 만들었습니다. 목적은 정확한 배출량이 아니라
**방문할 가치가 있는지 판단**하는 것입니다.

---

## 4. 파일별 역할

| 파일 | 역할 | 고칠 때 주의 |
|---|---|---|
| `juso.py` | 도로명주소 검색, 파라미터 변환 | 검색어 필터를 약하게 하지 말 것 |
| `portal.py` | 공공데이터포털 공통 호출·오류표 | 오류 문구는 시니어용 |
| `bldrgst.py` | 건축물대장 (동별 연면적·구조·지붕) | 전통 판정 규칙은 7장 참조 |
| `bldengy.py` | 월별 전기·가스 사용량 | 24회 호출 구조 |
| `emissions.py` | 배출량 계산 (5단계 산정식) | **계수는 6장 읽고 나서** |
| `template_out.py` | 엑셀 서식 채우기 | 노란 셀만. 수식·참조표 건드리지 말 것 |
| `store.py` | 사찰 기록 저장·비교 | 같은 지번·같은 해는 덮어씀 |
| `keys.py` | `키.txt` 에서 인증키 읽기 | **코드에 키를 넣지 말 것** |
| `app.py` | 화면 (Streamlit) 5단계 | 인증키는 `키.txt` 에 있음 |
| `test_key.py` | 인증키 확인 | 출력 문자 제한 있음 (7장) |
| `test_bldengy.py` | 대지/산 필터 확인. 인터넷·인증키 안 씀 | 출력 문자 제한 있음 (7장) |
| `test_store.py` | 기록 저장 확인. 임시 폴더에서만 돎 | 출력 문자 제한 있음 (7장) |
| `run.py` | 배치 파일이 부르는 시작 프로그램 | 한글 안내는 전부 여기 |
| `키확인.bat` `화면열기.bat` | 더블클릭 실행 | **한글 절대 금지** (7장) |

**코드가 아닌 파일 셋**

| 파일 | 내용 |
|---|---|
| `키.txt` | 인증키 두 줄. **배포할 때 빼고 주십시오** |
| `서식_온실가스_배출량_산정_템플릿.xlsx` | 원본 서식. **수정 금지** |
| `사찰기록.csv` | 저장된 사찰. 없으면 처음 저장할 때 생깁니다 |

---

## 5. 쓰는 API 세 가지

### 도로명주소 검색 API

- `https://business.juso.go.kr/addrlink/addrLinkApi.do`
- 실패 시 `https://www.juso.go.kr/addrlink/addrLinkApi.do` 로 자동 재시도
- 승인키는 `confmKey`, 개발계정

**이 API의 응답이 나머지 두 API의 입력이 됩니다.**

| 주소 API 응답 | → | 건물 API 요청 |
|---|---|---|
| `admCd` 앞 5자리 | → | `sigunguCd` |
| `admCd` 뒤 5자리 | → | `bjdongCd` |
| `lnbrMnnm` | → | `bun` (4자리 0채움) |
| `lnbrSlno` | → | `ji` (4자리 0채움) |
| `mtYn` (1=산) | → | `platGbCd` |

### 건축HUB 건축물대장 (15134735 / BldRgstHubService)

- `https://apis.data.go.kr/1613000/BldRgstHubService`
- 쓰는 오퍼레이션: `getBrTitleInfo`(표제부·동별), `getBrRecapTitleInfo`(총괄),
  `getBrJijiguInfo`(용도지역·구역)
- **요청에 `platGbCd` 를 넣을 수 있습니다.** 산 지번이 깔끔하게 걸러집니다.

### 건축HUB 건물에너지 (15135963 / BldEngyHubService)

- `https://apis.data.go.kr/1613000/BldEngyHubService`
- `getBeElctyUsgInfo`(전기), `getBeGasUsgInfo`(가스). 단위는 둘 다 **kWh**
- **`useYm`(YYYYMM)이 필수라 한 달씩 불러야 합니다.**
  1년치 = 전기 12회 + 가스 12회 = **24회**, 약 30초
- **요청에 `platGbCd` 가 없습니다.** 응답의 `platGbCd` 로 걸러야 합니다
- 공간 단위가 **지번 기준**이라 한 지번의 여러 전각이 합산되어 나옵니다
- 제외 대상: 단독주택, 200세대 미만 공동주택, 산업·수송·발전·열병합·충전용.
  **종교시설은 제외 대상이 아닙니다**

공통: `_type=json`, 개발계정 하루 10,000건, 초당 30건, 1회 최대 100건.

---

## 6. 계산 근거 — 5단계 산정식

> **이전 지침은 폐기되었습니다.** 이 장은 2026-09-13 에 통째로 갈렸습니다.
> 예전에는 "활동량 x 통합배출계수" 로 계산하고 전력계수 0.4747 을 못박아
> 두었으나, 「온실가스 배출량 산정 실무용 템플릿」이 **그 방식을 명시적으로
> 배격**합니다. 발열량이 어디에 반영되었는지 추적할 수 없고 값의 출처를
> 구분할 수 없기 때문입니다. **아래가 현행 기준입니다.**

### 산정식

```
배출량(tCO2eq) = 활동량 x 발열량 x 배출계수 x 산화계수 x GWP
```

- 연료를 부피·무게(L·kg·Nm3)로 사면 -> **발열량 적용 필수**
- 전기처럼 이미 에너지 단위(kWh)면 -> **발열량 불필요**, 배출계수 직접 적용
- **산화계수는 CO2 에만** 적용합니다 (CH4·N2O 에는 적용하지 않음)

### 참조표 (별표10·11·12)

**연료명은 템플릿 ②시트와 글자 하나까지 같아야 합니다.** 다르면 엑셀의
INDEX/MATCH 가 빈값을 돌려주고 계산이 통째로 0 이 됩니다.

| 연료명 | 단위 | 순발열량 MJ | CO2 kgCO2/TJ | CH4 | N2O | 산화계수 |
|---|---|---|---|---|---|---|
| 도시가스/LNG | Nm3 | 38.9 | 56100 | 1 | 0.1 | 0.995 |
| 경유(Diesel) | L | 35.2 | 73200 | 3 | 0.6 | 0.995 |
| 등유(Kerosene) | L | 34.2 | 73200 | 3 | 0.6 | 0.995 |
| 휘발유(Gasoline) | L | 30.4 | 71600 | 3 | 0.6 | 0.99 |
| LPG-프로판(LPG1호) | kg | 46.3 | 64600 | 1 | 0.1 | 0.99 |
| LPG-부탄(LPG3호) | kg | 45.7 | 66300 | 1 | 0.1 | 0.99 |

**GWP** — AR5(K-ETS 공식) CH4 28 / N2O 265, AR6(CBAM·ISSB) CH4 29.8 / N2O 273

### 전력 — 세 기준을 고를 수 있습니다

| 기준 | 값 | 비고 |
|---|---|---|
| **발전단(LB)** | **0.3844** | **기본값. 보고용 원칙** |
| 소비단(MB) | 0.4173 | RE100 등 참고용 |
| 보고서2024 | 0.4747 | 기존 5개 사찰 비교용만 |

### 도시가스 kWh -> Nm3 — **순발열량 38.9MJ 확정** (2026-09-14)

건물에너지는 kWh, 템플릿은 Nm3 을 요구합니다.

| 기준 | kWh/Nm3 | 수국사 가스 |
|---|---|---|
| **순발열량 38.9MJ (기본값)** | 10.8056 | **46.11 t** |
| 총발열량 43.1MJ | 11.9722 | 41.62 t |
| 보고서 역산 | 10.55 | 47.23 t |

**근거 둘.**

1. 240320 보고서가 봉은사 도시가스를 2,630,415 kWh = 249,328 Nm3 으로 적었습니다.
   나누면 10.550 kWh/Nm3 -> **37.98 MJ/Nm3**. 순발열량 38.9 와 +2.4%,
   총발열량 43.1 과는 +13.5% 차이입니다
2. **더 결정적인 것은 왕복입니다.** 총발열량으로 나누고 템플릿에서 순발열량을
   곱하면 **에너지가 9.7% 사라집니다**(825,325 -> 744,899 MJ).
   순발열량으로 나누면 정확히 보존됩니다. kWh 를 Nm3 안 거치고 바로 TJ 로
   바꾼 값(46.07 t)과도 순발열량 경로(46.11 t)가 맞습니다

고지서 실물 대조는 아직 남아 있어 세 선택지는 남겨 두었습니다.

### Scope 분류 — 연료가 아니라 쓰임으로 정합니다

| Scope | 내용 | 템플릿 시트 |
|---|---|---|
| 1 고정연소 | 도시가스, LPG, 등유 (보일러·취사) | ③ |
| 1 이동연소 | 휘발유, 경유 (차량) | ④ |
| 2 | 구매전력 | ⑤ |

**같은 연료도 쓰임에 따라 시트가 다릅니다.** 경유를 보일러에 쓰면 ③,
차량에 쓰면 ④입니다. 계수는 같습니다. 그래서 `template_out.Activity` 의
`scope` 는 부르는 쪽이 지정합니다.

상수도(0.332 kgCO2/톤)는 Scope 3 이라 이 템플릿에 없습니다.

### 검산 (2026-09-13 확인)

```
python -c "import emissions as e; [print(f, round(e.burn(f,1).ar5_t*1000,4)) for f in e.FUELS]"
```

| 연료 | kgCO2eq/단위 |
|---|---|
| 도시가스/LNG | 2.1735 |
| 경유(Diesel) | 2.5723 |
| 등유(Kerosene) | 2.4992 |
| 휘발유(Gasoline) | 2.1623 |
| LPG-프로판(LPG1호) | 2.9636 |
| LPG-부탄(LPG3호) | 3.0021 |

**수국사 전체** (전력 208,412 kWh / 가스 229,257 kWh / 휘발유 1,750 L /
경유 2,400 L, 발전단·**순발열량**·AR5):
③ 46.11 + ④ 9.96 + ⑤ 80.11 = **136.18 tCO2eq**.

**이것이 현재 검증 기준입니다.** 옛 240320 보고서 값 재현은 **더 이상 기준이
아닙니다.** 계수가 구버전이고 수국사 행에 11개월 오류도 있습니다(아래).
억지로 맞추려 하지 마십시오.

| 기준 | 값 |
|---|---|
| 계수 6종 | 위 표와 일치 |
| 수국사 전체 | **136.18 tCO2eq** |
| 엑셀 ⑥종합요약 D10 | **136.18** |

*이력* — 총발열량 기본값이던 2026-09-13 에는 131.69 였고, 엑셀 재계산으로
D10 = 131.692 를 확인했습니다. 도시가스 기준이 바뀌어 값이 올라갔습니다.

### 옛 기준과의 차이

수국사 전력이 98.93 t -> **80.11 t** 로 19% 줄어듭니다.
가스도 계수가 2.1508 -> 2.1735 로 바뀌어 옛 보고서 값을 그대로 재현하지는
못합니다. **기존 다섯 사찰과 비교할 때만** 전력 "보고서2024"(0.4747)를 쓰십시오.

### 원보고서에 계산 오류가 하나 있습니다

**수국사 행이 1~11월로만 계산돼 있습니다.** (옛 계수 기준)

- 전력 87.02 t <- 183,312 kWh (1~11월). 12개월은 208,412 kWh
- 가스 37.08 t <- 181,891 kWh (1~11월). 12개월은 229,257 kWh

다른 네 사찰은 12개월 전체로 정상 계산돼 있습니다.

### 공개자료 커버리지 — 화면에서 뺐습니다

전기+가스만으로 총 배출량의 몇 %를 잡는지 보고서 실측으로 계산한 값입니다.

| 사찰 | 유형 | 커버리지 |
|---|---|---|
| 수국사 | 도심 | 92.9% |
| 금산사 | 산중 | 89.6% |
| 실상사 | 산중 | 85.7% |
| 법문사 | 산중 소규모 | 65.9% |

**2026-09-13, 소계를 0.93 으로 나누는 보정을 화면에서 뺐습니다.** 표본 4개 중
둘(실상사·금산사)에서 대장 누락이 확인되어 전제가 흔들렸고, 소수점까지 찍힌
숫자를 내면 사람들이 그대로 믿기 때문입니다. 화면에는 소계만 보이고 나머지는
문장으로 밝힙니다.

`emissions.COVERAGE` 와 `Rough.estimated_total_t` 는 **지우지 않고 남겨** 두었습니다.
표본이 쌓이면 다시 검토합니다.

## 7. 지켜야 할 규칙

### 배치 파일에 한글을 넣지 말 것

윈도우 명령창이 배치 파일에 든 한글을 잘못 읽어 **실행이 통째로 깨집니다.**
실제로 `set PY=python` 줄까지 망가져 `-m` 만 남은 채 실행된 적이 있습니다.

`키확인.bat` `화면열기.bat` 은 영문 일곱 줄뿐이고, 한글 안내는 `run.py` 가 맡습니다.
이 세 파일은 항상 같이 다녀야 합니다.

### `test_key.py` 출력에 cp949 밖 문자를 쓰지 말 것

한글 윈도우 명령창은 cp949 로 찍습니다. `—`(긴 줄표)나 `✔` 는 cp949 에 없어서
**결과를 파일로 저장하면**(`python test_key.py > 결과.txt`) 마지막 단계에서 멈춥니다.

`-` 와 `[OK]` 로 바꿔 두었습니다. 되돌리지 마십시오.
다른 파일의 `—` 는 주석에만 있어 괜찮습니다.

### 전통 전각 0동은 "없다"가 아니라 "못 찾았다"

수국사 실제 조회에서 목조 전각이 **한 동도 나오지 않았습니다.**
보물을 모신 절인데 4동 전부 철근콘크리트로만 잡혔고, 연면적도 327㎡ 비었습니다.

`bldrgst.checks()` 가 이걸 경고합니다. 이 함수를 지우지 마십시오.

### 전기가 어느 지번에 잡히는지는 사람이 확인해야 한다

**2026-09-14 갱신.** 건물에너지는 건축물대장이 걸린 **대표 지번 하나에 합산**
되어 나오는 것으로 보입니다. 조사한 네 곳에서 **지번 하나로 사찰 전체 값과
맞았습니다.**

| 사찰 | 지번 하나 조회 | 보고서(사찰 전체) | 차이 |
|---|---:|---:|---:|
| 봉은사 | 2,316,510 | 2,310,661 | +0.3% |
| 수국사 | 211,844 | 208,412 | +1.6% |
| 실상사 | 348,369 | 352,170 | -1.1% |
| 금산사 | 1,037,930 | 1,117,733 | -7.1% |

부속지번 23건(금산사 20 · 봉은사 3)은 **전부 자료가 없었습니다**(2026-09-14 확인).
그래서 부속지번을 추가로 훑을 실익이 없습니다.

**다만 네 곳뿐이므로 맞는다고 단정하지 마십시오.** 이상치가 나오면 현장 확인으로
넘깁니다. 실상사가 518 m² 에 348,369 kWh 인 것은 전기가 부풀려진 게 아니라
**면적만 빠진 것**입니다.

*이전 서술 정정* — 예전에 "지번만으로 사찰 전체를 집어내는 일은 원리적으로
불가능" 이라고 적었으나, 실측은 대체로 맞는 쪽이었습니다.

도구가 할 일은 정확한 값을 내는 것이 아니라, **어느 지번에 얼마가 잡히는지 보여주고
"이 값은 ○○ 지번 기준" 이라고 밝히는 것**까지입니다. 자동으로 맞히려 들지 마십시오.

### 자료가 안 맞으면 0 을 돌려주고, 걸러냈다는 사실을 밝힌다

건물에너지 응답에서 대지구분이 요청과 다르면 **0 을 돌려줍니다.** 남의 건물
전기를 합치는 것보다 0 이 낫습니다. 다만 **"자료 없음" 과 "걸러냄" 은 다릅니다.**
`fetch_month` 가 (사용량, 걸러낸 건수) 를 돌려주고 화면이 건수를 밝힙니다.

걸러냈다는 사실 자체가 단서입니다. 같은 번지에 다른 건물이 있다는 뜻이고,
사찰 전기가 그쪽에 물려 있을 수 있습니다. 숨기지 마십시오.

### 면적당 전기 200 kWh/m2 초과는 대장 누락 의심

| 사찰 | 연간 전기 | 대장 연면적 | kWh/m2 |
|---|---:|---:|---:|
| 법문사 | 17,647 | 1,473.3 | 12 |
| 수국사 | 211,844 | 2,658.3 | 80 |
| 봉은사 | 2,316,510 | 27,271.1 | 85 |
| 금산사 | 1,037,930 | 7,545.2 | 138 |
| 실상사 | 348,369 | 518.0 | **672** |

**실상사만 5배 이상 튑니다.** 518 m2 건물이 연간 348,369 kWh 를 쓸 수는 없습니다.
`emissions.area_check()` 가 200 을 넘으면 경고합니다. 기존 네 사찰은 12~138 이라
오탐이 없습니다. **전기가 부풀려진 것이 아니라 면적이 모자란 쪽**입니다.

### 전력 계수 선택은 엑셀에 반영되지 않는다 — 밝히고 쓸 것

**2026-09-14 확인.** `template_out.fill()` 은 엑셀에 **kWh 만** 씁니다(⑤시트 D7).
계수는 서식 안에 **0.3844 로 박혀 있고** 엑셀이 스스로 계산합니다.
D8 은 `=D7` 이라 소비단(MB)도 서식이 알아서 냅니다.

그래서 화면에서 「보고서2024」(0.4747)를 고르면 **화면 숫자와 내려받은 엑셀이
어긋납니다.** 서식의 수식·참조표는 고치지 말라는 규칙이 있으므로
**맞출 방법이 없습니다.** 대신 기본값이 아닌 것을 고르면 화면이 그 사실을
밝힙니다(4단계 「자세한 설정」).

**도시가스 기준은 반대로 제대로 반영됩니다.** Nm3 환산값 자체를 바꿔
활동량 칸에 넣기 때문입니다.

계수 고르기는 **접어 두었습니다.** 기본값이 옳고 대부분 손댈 일이 없는데
눈에 띄는 자리에 두면 시니어에게 판단을 요구하는 셈이 됩니다(2장).

### 남에게 보여주는 판에서는 1~4단계만 보인다

`JISU_SHARE=1` 이거나 Streamlit 비밀 보관함에 `share_mode = true` 이면
**주소 찾기 -> 건물 -> 개략 진단 -> 엑셀** 까지만 보입니다.
사찰 기록 저장·일괄 조회·방문 우선순위는 통째로 감춥니다. 까닭 둘.

1. 조사한 사찰 목록은 **모임 안의 자료**입니다. 남에게 보일 것이 아닙니다
2. 웹에 올리면 여러 사람이 **같은 파일에 쓰게 되어** 서로의 기록이 섞이고,
   서버가 다시 뜨면 사라집니다. 기록은 교수님 컴퓨터에서만 쌓습니다

인증키도 웹에서는 `키.txt` 대신 **Streamlit 비밀 보관함**에서 읽습니다
(`keys._from_secrets()`). 파일로 두면 GitHub 에 딸려 올라갈 수 있습니다.

**개인정보는 도구에 두지 않습니다** (작업지시 03 3장).
`녹색사찰_명단.csv` 는 **번호·사찰명·주소 세 열뿐**이고, `store.COLUMNS` 와
`batch.py` 에서도 주지스님·연락처·종단을 뺐습니다(2026-09-14). 한 번
들어갔다가 뺀 것이니 **다시 넣지 마십시오.** 명단 CSV 는 `.gitignore` 에
있습니다. 조회 입력 자료이지 코드가 아닙니다.

### 태양광 설치 가능 여부를 단정하지 말 것

도구는 **필요용량과 이론 발전량까지만** 냅니다.
보고서에 실패 사례가 둘 있습니다. 금산사는 계통 확보에 5년이 걸린다는 답을 듣고
포기했고, 실상사는 산중 입지와 경관 문제로 어려웠습니다.
지도나 계산으로는 절대 안 나오는 사유입니다.

### 추정치에는 근거를 함께 표시할 것

보고서 스스로 "전기 난방을 병행하면 난방 배출량을 정확히 집계하기 어렵다"고
밝혔습니다. 추정한 값은 추정이라고 화면에 적어야 합니다.

---

## 8. 실제 검증 결과 (2026-09-12)

### 된 것

| 항목 | 결과 |
|---|---|
| 도로명주소 승인키 | 정상 |
| 공공데이터포털 인증키 | 정상 |
| 주소 → 파라미터 변환 | 정상 (수국사 11380 / 10500 / 0314 / 0000) |
| 건축물대장 조회 | 정상 |
| 전통·신식 분류 | **봉은사 26동 중 21동 전통 정상 분류** |
| 계산 엔진 | 보고서 4개 값 소수점까지 재현 |

### 건축물대장을 믿을 수 있는 범위

| | 건축물대장 | 보고서 | 전통 전각 |
|---|---|---|---|
| 봉은사 | 26동 · 27,271 m² | 26,786 m² | 21동 정상 |
| 수국사 | 4동 · 2,658 m² | 2,985.6 m² | **0동 (누락)** |

봉은사는 1.8% 이내로 맞지만 수국사는 11% 빕니다.
**절마다 편차가 큽니다.**

### 산 지번 (2026-09-12 검증 완료)

`대자동 산162` 로 화면 전 구간을 확인했습니다.

**이곳이 법문사 본절입니다**(2026-09-14 정정). 이 문서는 "법문사 사찰림" 으로
적었으나, 녹색사찰 명단 51번 법문사 주소가 `경기도 고양시 덕양구 통일로 654
(대자동)` 이고 **`대자동 산162` 의 도로명이 바로 그 주소**입니다.
이름 검색이 낸 `파주시 법원읍 웅담리 산4-1 법문사` 는 **다른 절**입니다.

| 항목 | 결과 |
|---|---|
| 목록 **(산)** 표시 · `· 산 지번` 제목 · 경고 | 모두 정상 |
| 건축물대장 `platGbCd=1` 조회 | 2동 591 m² 정상 |
| 사찰 유형 | **「산중」으로 자동 전환** (커버리지 0.88) |
| 건물에너지 24회 호출 | 완주. 용도지역 자연녹지·개발제한구역 |

**실상사는 산 지번이 아닙니다.** `입석리 50-1` 대지(`platGbCd=0`) 입니다.
**이 문서가 실상사를 산 지번 검증 대상으로 적은 것은 잘못이었습니다**(2026-09-13 정정).
법문사 사찰림 `대자동 산162` 로 검증을 마쳤습니다.
`실상사` 라는 **절 이름으로도 9건이 검색됩니다** (README 설명과 다름).

### 대지/산 필터 (2026-09-13 해결)

건물에너지는 요청에 `platGbCd` 가 없어 응답에서 걸러야 합니다(5장).
`162`(대지)와 `산162`(산)는 둘 다 `bun=0162` 라 한 응답에 섞여 올 수 있습니다.

**옛 코드에 버그가 있었습니다.**

```python
picked = [r for r in rows if r.platGbCd == str(plat_gb)]
rows = picked or rows      # <- 걸러서 비면 남의 건물을 그대로 씀
```

산 지번 사찰을 조회했는데 그 사찰엔 자료가 없고 **같은 번지 대지 건물에만**
자료가 있으면, 남의 전기 사용량이 사찰 것으로 합산되었습니다.
숫자가 조금 틀리는 정도가 아니라 **"자료 없음 -> 방문 필요"가 "신뢰도 높음"으로
뒤집혔습니다.** 2장 "모르는 것은 모른다고 표시한다" 위배입니다.

응답이 대지구분을 밝혔으면 그 말을 믿고, 안 맞으면 0 을 돌려주도록 고쳤습니다.
대지구분을 **아예 안 밝힌** 응답에만 예전처럼 전부 씁니다(원래 의도는 이것이었음).

`test_bldengy.py` 가 열두 갈래를 확인합니다(걸러낸 건수 포함). 옛 코드에 대고 돌리면 정확히
문제의 두 갈래만 실패하므로, 이 테스트는 헛돌지 않습니다.

**다만 겹치는 지번을 실물에서는 아직 못 봤습니다.** 산 지번 5곳과 대상 사찰
6곳을 조회했는데 응답이 늘 한 종류였고, `platGbCd` 는 6/6 실려 왔습니다.
코드는 고쳤으나 **이런 상황이 현실에 얼마나 흔한지는 모릅니다.**

### 건물에너지 — 보고서 대조 (2026-09-12 검증 완료)

| 사찰 | 조회값 | 보고서 | 차이 |
|---|---:|---:|---:|
| 봉은사 | 2,316,510 (2023) | 2,310,661 | **+0.3%** |
| 수국사 | 211,844 (2022) | 208,412 | **+1.6%** |
| 금산사 | 1,037,930 (2024) | 1,117,733 | **-7.1%** |
| 실상사 | 348,369 (2024) | 352,170 | **-1.1%** |

**금산사·실상사는 2022년 자료가 0이고 2023년도 일부뿐입니다. 2024년으로 보십시오.**
24회 호출은 실제로 돌며 30~40초 걸립니다.

---

## 9. 남은 과제 (우선순위)

### 1순위 — 검증 (2026-09-12 완료, 8장에 결과)

- [x] 산 지번 사찰 조회. `platGbCd=1` 경로 — `대자동 산162` 로 전 구간 통과
- [x] 건물에너지 조회 — 네 사찰 모두 보고서와 10% 안쪽
- [x] 건물에너지 대지/산 필터 — 폴백 버그 수정 + `test_bldengy.py` (2026-09-13)
- [ ] **남은 것** — 겹치는 지번이 현실에 얼마나 흔한지. 코드는 고쳤으나 빈도 미상

### 2순위 — 사람이 정해야 함 (10장)

- [x] 전력 배출계수 — **0.3844(발전단 LB) 확정** (2026-09-13)
- [x] 면적 기준 — 대장 값 그대로 + 어긋남 표시 + 원단위는 누락 없을 때만
- [x] 커버리지 보정 — 화면에서 제거 (6장)
- [ ] **남은 것** — 도시가스 kWh -> Nm3 기준. 고지서 한 장이면 확정

### 3순위 — 기능

- [x] 인증키를 코드에서 분리 — `키.txt` + `keys.py` (2026-09-13)
- [x] 진단 결과를 문서로 출력 — 엑셀 서식 (`template_out.py`, 4단계 화면)
- [x] 사찰 여러 곳을 저장·비교 — `store.py` + 5단계 화면 (2026-09-14)
- [ ] 계약전력 분석 — **고압·저압에 따라 로직이 정반대**임에 주의.
      실상사는 계약전력이 실사용의 2.6배인데도 조정 불필요 판정을 받았고,
      저압인 법문사는 60kW→15kW 조정으로 연 333만원을 실제로 줄였습니다

### 안 할 것

- 고지서 OCR — 서식이 제각각이라 정확도를 보장할 수 없습니다.
  사진은 근거 보관용으로만 두고 숫자는 사람이 봅니다
- 문화재 현상변경 판정 자동화 — 관할 지자체 질의로만 확정됩니다

---

## 10. 사람이 정해야 할 것

아래는 코드로 결정할 수 없습니다. **김창식 교수(경영지도사·환경금융 석사)에게
물어보십시오.** 보고서 맥락을 아는 쪽에서 정해야 합니다.

### 정해진 것 (2026-09-13)

| 항목 | 결정 |
|---|---|
| **전력 배출계수** | **0.3844 발전단(LB) 기본값.** 소비단 0.4173 과 보고서2024 0.4747 도 고를 수 있게 남김 |
| **커버리지 보정** | **화면에서 제거.** 소계만 보이고 나머지는 문장으로. 상수는 남겨 둠 (6장) |
| **면적 기준** | 대장 값 그대로 + 어긋남 표시 + 원단위는 누락 없을 때만 (아래 2번) |
| **인증키** | `키.txt` 설정 파일 방식 |
| **다음 기능** | 엑셀 서식 출력이 1순위 -> 완료. 다음은 사찰 목록 저장·비교 |

### 아직 정해야 할 것

1. **도시가스 kWh -> Nm3 기준** — 총발열량 43.1MJ 을 기본값으로 두었습니다.
   고지서 한 장에 kWh 와 Nm3 이 함께 찍혀 있으면 나눠 보아 확정됩니다.
   세 기준의 차이는 수국사 가스 기준 41.62 / 46.11 / 47.23 t 입니다
2. **면적 기준** — 아래 표를 근거로 2026-09-13 에 방향은 정해졌습니다.
   남은 것은 원단위를 언제부터 내놓을지입니다.

   네 사찰 실측 (법문사는 보고서 연면적을 몰라 제외):

   | 사찰 | 동 | 동별합계 | 주건축물만 | 총괄표제부 | 보고서 |
   |---|---:|---:|---:|---:|---:|
   | 봉은사 | 26 | 27,271.1 | 27,271.1 | 27,271.1 | 26,786.0 |
   | 수국사 | 4 | 2,658.3 | 2,606.1 | 2,606.1 | 2,985.6 |
   | 금산사 | 34 | 7,545.2 | 6,599.6 | **5,469.6** | **5,470.0** |
   | 실상사 | 10 | 518.0 | 518.0 | 518.0 | 1,809.3 |

   보고서 대비: 봉은사 +1.8% / 수국사 -11~-12.7% / 실상사 **-71.4%** (세 기준 모두 같음)
   금산사만 기준별로 +37.9% / +20.7% / **-0.0%** 로 갈립니다.

   **읽어낸 것 셋**

   - **기준 선택은 별로 중요하지 않습니다.** 봉은사·실상사는 세 기준이 완전히 같고
     (부속건축물 0동), 수국사도 52.2 m² 차이뿐입니다. 갈리는 건 금산사 하나입니다
     (부속 11동 945.6 m², 총괄표제부가 동별합계보다 2,076 m² 작음)
   - **진짜 문제는 대장의 누락입니다.** 수국사 -11%, 실상사 -71% 는 어느 기준을
     골라도 그대로입니다. 7장의 "전통 전각 0동은 못 찾은 것"이 면적에서도 같은
     모습으로 나타납니다
   - **보고서 연면적도 출처가 섞여 있는 듯합니다.** 금산사만 총괄표제부와 소수점까지
     일치합니다. 금산사는 대장에서, 수국사·실상사는 다른 데서 가져온 것으로 보입니다.
     그렇다면 "건축물대장 vs 보고서" 는 같은 기준끼리의 비교가 아닙니다.
     정황이지 확증은 아니므로, 원보고서(11장)를 보면 판가름 납니다

   **정해진 방향** — 기준을 하나 고르는 문제가 아닙니다. 실상사를 518 m² 로 나누면
   원단위가 3.5배 부풀고 이는 어느 기준에서나 같습니다. 대장 값을 그대로 쓰되
   어긋남을 표시하고(`bldrgst.checks()` 가 이미 함), **원단위는 누락이 확인되지 않은
   사찰에서만** 내놓습니다. 2장 "모르는 것은 모른다고 표시한다" 그대로입니다.

   **법문사 연면적은 아직 모릅니다.** 본절 주소와 보고서 연면적이 확인되면
   표에 채워 넣으십시오. 커버리지 65.9% 로 가장 작은 사찰이라 표본 가치가 있습니다.
3. **웹 배포 여부** — 모임 여러 명이 각자 쓰게 할지.
   인증키는 `키.txt` 로 분리해 두었으므로 준비는 되었습니다
4. **법문사 주소 — 풀렸습니다** (2026-09-14). 녹색사찰 명단 51번이
   `경기도 고양시 덕양구 통일로 654 (대자동)` 이고, 이는 이미 조회해 본
   `대자동 산162` 의 도로명입니다. **사찰림이 아니라 본절이었습니다.**
   이름 검색이 낸 파주 웅담리는 같은 이름의 다른 절입니다.
   연면적은 대장에 591 m² 뿐이라 누락이 의심됩니다(7장 기준 미달이지만
   전기가 적어 경고는 안 뜹니다)

---

## 11. 참고 자료

프로젝트 폴더 밖에 있는 원자료입니다. 숫자를 확인할 일이 생기면 요청하십시오.

- `240320_불교사찰_탄소중립_최종보고서.pdf` — 5개 사찰 실측, 25개 교구본사 전력
- `사찰_넷제로_추진검토.docx` — 3층 구조(태양광→히트펌프→사찰림), 계약전력 사례
- `claude_마곡사_태양광_사업구조_검토결과.md` — Track A/B 재무모델
- `온실가스_배출량_계산식.xlsx` — 현재 쓰는 엑셀. 계수 출처
- `2_탄소중립첫걸음워크시트_단체용_수정.pdf` — 태양광 필요용량 산식, 해줌 안내
- `검증할주소.md` — 넣어 볼 주소와 정답 (같은 폴더)

---

## 12. 작업 기록

새로 작업하실 때마다 아래에 덧붙여 주십시오.
날짜 / 무엇을 / 왜 / 확인한 것 순으로 적으면 됩니다.

### 2026-09-12

- `test_key.py` 출력문 6곳을 cp949 안전 문자로 교체 (`—`→`-`, `✔`→`[OK]`).
  결과를 파일로 저장할 때 멈추던 문제 해결. 로직은 건드리지 않음
- 배치 파일에서 한글 제거, `run.py` 신설. 한글 때문에 배치 실행이 깨지던 문제 해결
- `bldrgst.checks()` 추가. 전통 0동·종교시설 없음·동 이름 없음·연면적 불일치 경고
- 동 이름이 빈 경우 주용도와 층수로 보완하도록 `name()` 개선
- 수국사·봉은사 실제 조회 성공. 두 인증키 정상 확인

### 2026-09-12 (두 번째)

- **파이썬 3.14.6 에 streamlit 1.63.0 · requests 2.34.2 설치.**
  기본 `python` 이 Anaconda 3.9.7 인데 streamlit 이 이 버전만 콕 집어 제외합니다
  (`Requires-Python >=3.9, !=3.9.7`). Anaconda 두 개는 건드리지 않았습니다
- **배치 파일 버그 수정.** `if exist` 가 순서대로 덮어써서 마지막 줄인
  `%USERPROFILE%\anaconda3`(3.9.7·streamlit 깨짐)가 이기고 있었습니다.
  `if not defined` 가드를 넣어 **먼저 찾은 것이 이기도록** 바꾸고 3.14 를 1순위로.
  한글은 그대로 넣지 않았습니다(7장)
- **월별 전기 그래프 x축 정렬 수정.** `f"{int(k[4:])}월"` 이 문자열로 정렬돼
  `10월 11월 12월 1월 2월…` 순으로 나왔습니다. `f"{k[4:]}월"`(01월~12월)로 교체
- **「산 지번만 찾기」 단추 추가.** `산내면 입석리` 는 148건 30쪽인데 산 지번이
  **19쪽에 1건**뿐이라 시니어가 도달할 수 없었습니다(2장 위배).
  목록에 (산) 이 하나도 없을 때만 단추가 뜨고, 누르면 검색어 끝에 "산"을 붙여
  다시 찾습니다. 저 혼자 움직이지 않습니다(2장)
- 9장 1순위 검증 완료. 8장에 결과 기록

### 2026-09-13 (첫 번째)

- **`bldengy.fetch_month` 의 `picked or rows` 폴백 수정.** 산 지번 사찰을 조회했는데
  같은 번지 대지 건물 자료만 오면 그것을 사찰 것으로 합산하던 문제입니다.
  없는 자료가 그럴듯한 숫자로 둔갑하고 신뢰도까지 올라갔습니다(2장 위배).
  응답이 대지구분을 밝혔는데 안 맞으면 0, 아예 안 밝혔을 때만 전부 사용
- **`test_bldengy.py` 신설.** 가짜 응답으로 아홉 갈래 확인. 인터넷도 인증키도
  쓰지 않습니다(키 사본을 늘리지 않으려고 일부러 뺐습니다).
  **옛 코드에 대고 돌리면 문제의 두 갈래만 실패하는 것까지 확인했습니다**
- 실물 회귀 확인 — 수국사 2022년 211,844 kWh, 실상사 2024년 348,369 kWh.
  수정 전과 동일. 기존 조회 결과는 달라지지 않았습니다
- **면적 기준 판단 재료 수집.** 네 사찰 x 세 기준(동별합계·주건축물만·총괄표제부)을
  보고서와 대조. 10장 2번에 표와 결론 기록. 기준 선택보다 대장 누락이 문제라는 것,
  보고서 연면적도 출처가 섞인 듯하다는 것이 드러났습니다
- **7장에 규칙 추가** — 전기가 부과되는 지번은 사람이 확인해야 한다.
  계량기 단위로 묶여 부과되므로 지번만으로 자동 판별은 불가능합니다
- **화면이 7장 새 규칙을 지키도록 `app.py` 세 곳 수정.** 전에는 숫자만 있고
  그것이 어느 지번 것인지 한 줄도 없었습니다. 실상사 348,369 kWh 를 사찰 전체로
  읽으면 그대로 틀립니다
  - 조회 단추 **위**: 지번 한 곳 기준이라는 것과, 고지서 주소가 가장 정확하다는 안내
  - 결과 **바로 아래**: "이 값은 OO 지번에 잡힌 것만 더한 것입니다. 사찰 전체가
    아닐 수 있습니다" (지번 이름이 실제로 박혀 나옵니다)
  - 자료 없을 때: "이 지번에 없을 뿐, 다른 지번으로 잡혀 있을 수 있습니다" 로 바꿔
    다음 행동을 붙였습니다(2장). 전에는 바로 "방문해서 고지서를 받아야 합니다" 였음

### 2026-09-13 (작업지시 02)

`작업지시_완료_02_20260913.md` 의 지시를 이행했습니다.

**계산부 5단계 개정 (가장 큰 변경)**

- `emissions.py` **전면 개작.** 활동량 x 발열량 x 배출계수 x 산화계수 x GWP.
  `FUELS` 참조표(별표10·11·12), `burn()`, `power_t()`, `gas_kwh_to_nm3()`,
  GWP AR5/AR6, 전력 3기준, 도시가스 3기준
- 검산 통과 — 연료 6종 계수 전부 일치, 수국사 131.69 tCO2eq 재현
- **엑셀로 실제 계산시켜 ⑥종합요약 D10 = 131.692 확인** (Excel COM 재계산)
- 옛 `Result` 는 `Rough` 로 바뀌었고 `estimated_total_t`·`COVERAGE` 는 남겨 둠

**엑셀 서식 출력**

- `template_out.py` 는 받은 그대로 씁니다. **고친 곳 없습니다**
- `app.py` 에 4단계 절 추가 — 계수 고르기, 현장 조사값 4칸, 미리보기,
  Scope 합계, 산식 보기, `st.download_button`
- `requirements.txt` 에 `openpyxl>=3.1`

**그 밖**

- `bldengy.fetch_month` 가 (사용량, **걸러낸 건수**) 를 돌려주도록. 화면이 건수를
  밝힙니다. `fetch_year` 결과에 `dropped` 키가 생겼습니다
- `emissions.area_check()` — 면적당 전기 200 kWh/m2 초과 시 대장 누락 경고.
  **실상사 685 kWh/m2 로 경고 뜨고, 수국사는 안 뜨는 것까지 화면에서 확인**
- 커버리지 보정을 화면에서 제거. 지표가 "공개자료 배출량"(소계)으로 바뀜
- **인증키 분리** — `키.txt` + `keys.py` 신설. `app.py`·`test_key.py` 에서 키 제거
- `test_bldengy.py` 12갈래로 늘림(걸러낸 건수 확인 3건 추가). 전부 통과

**8·9번 시험 결과 (지시서가 알려 달라고 한 것)**

- **부속지번(`getBrAtchJibunInfo`) — 작동하지만 실익 없음.** 금산사 20건,
  봉은사 3건이 나오는데 **그 지번들의 전기가 전부 0** 입니다. 본지번 값도
  안 변합니다(금산사 144,517 -> 144,517). 수국사·실상사·법문사는 부속지번 자체가
  없습니다. **건물에너지는 대표 지번 하나에 이미 전부 합산**되어 있습니다.
  그래서 부속지번 활용 기능은 만들지 않았습니다
- 이 결과로 **실상사 518 m2 에 348,369 kWh 가 설명됩니다.** 전기가 부풀려진 게
  아니라 면적만 빠진 것이고, 네 사찰 모두 지번 하나로 보고서 값과 맞습니다
- **절 이름 검색 — 5곳 중 4곳 성공.** 수국사·실상사·법문사는 1쪽 상위,
  봉은사는 1,905건이지만 1쪽 첫 줄이 정답, **금산사만 실패**(270건,
  `금산면` 때문에 쌍용리가 밀려 올라옴). 안내 문구에 넣을 만합니다

**지시서에 없지만 고친 것 하나**

- `portal.request` 가 **비-JSON 응답에서 재시도 없이 포기**했습니다. 작업 중
  건축물대장 API 가 HTML 오류 쪽지를 보내 `test_key.py` 가 두 번 연속 실패하고
  세 번째에 성공했습니다. 화면 문구도 "서버가 예상과 다른 형식으로 답했습니다"
  로 끝나 **다음 행동이 없었습니다**(2장 위배)
- 연결 실패와 똑같이 다시 시도하도록 바꾸고(기본 3회), 문구에 "1~2분 뒤에 다시
  눌러 보세요" 를 붙였습니다. `bldengy` 도 3회로 맞췄습니다
- 가짜 응답으로 재시도가 실제로 도는 것(1번 실패 -> 2회 호출, 2번 실패 -> 3회 호출)과
  실물 `test_key.py` 3회 연속 성공을 확인했습니다

**기존 작업과 충돌한 부분**

- `app.py` 3단계는 "건드리지 말라"고 하셨으나, 지시 4·5·6번이 모두 3단계
  화면이라 불가피하게 손댔습니다. 1·2단계와 4단계 신설부는 그대로입니다
- 지시서 Q5 의 "인접 지번 훑기"는 위 8번 결과로 전제가 흔들려 **보류**했습니다

### 2026-09-14

**사찰 목록 저장·비교** (9장 3순위, 작업지시 02 의 Q7 에서 지정한 다음 1순위)

지금까지는 한 사찰 보고 나면 아무것도 안 남았습니다. 50개 로드맵이 목표인데
매번 처음부터 다시 찾아야 했습니다.

- `store.py` 신설 — 같은 폴더의 `사찰기록.csv` 에 한 줄씩 쌓습니다 (28개 열)
- **CSV 를 고른 이유** — 시니어가 엑셀로 바로 열 수 있어야 합니다(2장).
  **UTF-8 BOM** 으로 저장해야 엑셀이 한글을 안 깨뜨립니다
- **같은 지번·같은 해는 덮어씁니다.** 줄이 쌓이면 어느 것이 최신인지 사람이
  판단해야 하는데 그건 2장 위배입니다
- 덮어쓰기 전에 `.bak` 을 남깁니다. 엑셀에서 파일을 열어 둔 채 저장하면
  실패하므로, 그때는 "엑셀에서 닫고 다시 눌러 주세요" 로 안내합니다
- `app.py` 5단계 — 사찰 수·배출량 평균·면적당 전기 평균, 비교표, CSV 내려받기,
  한 줄 지우기. 3단계 끝에 「이 사찰 기록에 저장하기」 단추
- **벤치마크는 대장 누락 의심 사찰을 면적당 평균에서 뺍니다**(7장).
  실상사처럼 685 kWh/m2 짜리가 섞이면 평균이 무의미해집니다
- `test_store.py` 신설 — 17갈래. 임시 폴더에서만 돌아 실제 기록을 건드리지
  않습니다. 기록이 날아가면 되돌릴 수 없어 따로 확인합니다

**화면 확인** — 수국사(117.02 t / 78 kWh/m2 / 누락 아니오)와
실상사(136.44 t / 685 / 누락 예)를 실제로 저장해, 표에 쌓이는 것과
**면적당 평균이 78 로 나오는 것**(실상사 685 를 제외)을 확인했습니다.

**문서 정리도 함께**

- 4장 파일표가 낡아 있었습니다. `app.py` 에 "인증키가 여기 들어 있음" 이
  남아 있었고(이제 `키.txt`), `keys.py`·`template_out.py`·`store.py`·
  `test_store.py` 가 빠져 있었습니다. 코드가 아닌 파일 셋도 표로 넣었습니다
- 8장의 "아홉 갈래" 를 "열두 갈래" 로 (걸러낸 건수 확인이 늘어남)
- **12장 순서가 거꾸로였습니다.** 같은 날 먼저 한 작업이 나중 작업 뒤에
  놓여 있어 `(첫 번째)` 를 붙이고 앞으로 옮겼습니다


### 2026-09-14 (작업지시 03) — 진행 중

`작업지시_03.md` 이행. **1~7번 완료, 8·9번 남음.**

**1. 도시가스 기본값 -> 순발열량 38.9MJ** (6장에 근거)
왕복 검산으로 확정했습니다. 총발열량으로 나누고 템플릿에서 순발열량을 곱하면
에너지가 **9.7% 사라집니다**(825,325 -> 744,899 MJ). 순발열량은 정확히 보존됩니다.
수국사 합계 131.69 -> **136.19 t** (지시서 136.18 과 0.01 차이는 반올림).

**2. `CLAUDE.md` 정정** — 6장(도시가스·검증기준), 7장(계량기 규칙),
8장·10장(법문사). **법문사는 `대자동 산162` 가 본절이었습니다.**
명단 51번 주소 `통일로 654 (대자동)` 이 그 지번의 도로명입니다.

**3. 이름 검색** — `bdNm` 이 **딱 맞는 것**을 먼저 올립니다. 그냥 "들어갔는가"
로만 보면 수국사 검색에 **공중화장실**이 1위로 왔습니다. 검색창 안내를
"주소 또는 절 이름" 으로 바꾸고 `(이름으로 찾음)` 표시와 동명이사 경고를 붙였습니다.

> **금산사는 정렬로 고칠 수 없습니다.** 270건을 전수 훑었는데 `금산면 금산리 39`
> 의 **건물명이 비어 있습니다**. 이름으로 나온 12건은 전부 다른 절이거나
> `(주)금산사`·`금산사슴농장` 같은 업체입니다. **정답이 목록에 없습니다.**
> 지시서는 "밀려난 것" 으로 보았으나 실제로는 자료에 없는 것입니다.

**4. `store.py` 열 보강** — 28 -> **37열**. 명단 열과 방문 관리 열(상태·메모)을
넣고 중간값(국민 환산·가스 Nm3·전기/가스 배출량 따로)을 뺐습니다.

- **사람이 채운 칸은 재조회해도 지킵니다**(`KEEP_ON_UPDATE`)
- **같은 곳인지 보는 기준을 바꿨습니다.** 명단 번호가 있으면 번호가 정체성입니다.
  주소로 보면 첫 시도 실패(명단 주소)와 재시도 성공(실제 지번)이 **두 줄**이 됩니다.
  실제로 참당암이 그랬습니다

**5·6. 일괄 조회와 방문 우선순위** — `batch.py` 신설, `app.py` 6·7단계.

- 10곳씩. **한 곳이 끝날 때마다 저장**하므로 끊겨도 이어집니다
- 이미 한 곳은 건너뛰고, 「실패한 곳만 다시」 가 따로 있습니다
- 우선순위는 **점수를 합치지 않습니다**(2장). 배출 규모·난방 부담·태양광 여지를
  나란히 놓고 배출 규모 순으로 줄만 세웁니다
- **신뢰도 낮음·대장 누락은 「판단 보류」 로 따로 뺍니다.** 우선순위가 낮은 게
  아니라 자료가 부족한 것입니다

**7번 확인 (5곳 시험)**

| 번호 | 사찰 | 결과 |
|---|---|---|
| 1 | 금륜사 | 36.79 t · 난방 20.6배 |
| 6 | 법장사 | 19.87 t |
| 51 | 법문사 | 3.22 t |
| 36 | 참당암 | 62.42 t · 대장 누락 의심 |
| 25 | 현덕사 | **건물 없음 · 에너지 자료 없음** |

- 정상일 때 **한 곳 10~20초**. 5곳 45초
- 두 번째로 돌리니 **5곳 전부 건너뜀** (이어하기 정상)
- **주소에 괄호가 있으면 0건이 됩니다.** `(삼인리, 참당암)` 이 검색어에 섞여
  참당암이 실패했습니다. **명단 10곳이 괄호를 갖고 있습니다.**
  괄호를 뗀 것을 먼저 쓰도록 고쳐 해결했습니다
- 현덕사는 주소는 찾히나 **건축물대장에 건물이 없습니다**. 미등재 사찰입니다

**한 곳에 34분을 쓴 일 — 시간 상한을 넣었습니다**

두 번째 시험에서 금륜사 한 곳에 **2,042초**가 걸렸습니다. 그때 제공기관 서버가
아팠고, 24회 x 재시도 3회 x 타임아웃 15초가 겹친 것입니다. 나머지 4곳은 각 10초.

69곳이면 몇 시간이 될 수 있어 **한 곳에 3분 상한**을 두었습니다(`batch.BUDGET`).
넘으면 건너뛰고 다음으로 갑니다. 일괄 조회의 건물에너지 호출은 타임아웃 8초·
재시도 2회로 줄였습니다.

**7. 보기부가 인증키 없이 도는지** — 확인했습니다. `store.py` 는 `juso`·`portal`·
`bldengy`·`keys` 를 **하나도 import 하지 않습니다.** CSV 만 있으면 벤치마크와
방문 우선순위가 전부 나옵니다. 웹 2단계(보기 전용) 조건이 섭니다.

**8. README 갱신** — 통째로 다시 썼습니다. 낡은 정도가 심했습니다.
`키.txt` 안내가 없고, 4~7단계가 통째로 빠졌고, "인증키는 app.py 맨 윗부분에"
라는 **틀린 설명**과 옛 계수 0.4747 이 그대로 있었습니다. 새 사람이 보면
바로 틀리는 상태였습니다. 이름 검색의 동명이사 위험과 금산사가 안 나오는 것,
`사찰기록.csv` 의 연락처를 밖으로 내지 말라는 것도 넣었습니다.

**9. CSV 병합** — `store.merge()` + 5단계의 「나눠 돌린 기록 합치기」.
여러 CSV 를 올리면 같은 곳·같은 해는 **저장시각이 늦은 것**만 남깁니다.
사람이 적어 둔 방문 상태·메모는 합칠 때도 지킵니다.
`test_store.py` 에 5갈래 추가(총 27갈래).

**의견 받기용 배포 모드** — 모임 회원들께 보여 드리려고 만들었습니다.
`JISU_SHARE=1` 또는 비밀 보관함 `share_mode = true` 이면 1~4단계만 보입니다.
5·6·7단계(기록·일괄조회·우선순위)와 3단계의 저장 단추를 감춥니다.
`keys.py` 가 Streamlit 비밀 보관함도 읽도록 했습니다(웹 배포용).
두 판을 동시에 띄워 둘 다 오류 없이 뜨는 것을 확인했습니다.

**배출계수 선택을 접고 어긋남을 밝혔습니다** — 왜 선택으로 두었는지 물으셔서
확인하다 **화면과 엑셀이 어긋나는 것**을 찾았습니다(7장에 규칙으로 기록).
전력 계수 선택은 화면에만 듣고 엑셀은 늘 0.3844 로 계산합니다.
서식 수식을 못 고치니 맞출 수 없어, 기본값이 아닌 것을 고르면 그 사실을
경고로 밝힙니다. 두 선택 모두 「자세한 설정」으로 접었습니다(2장).

**낡은 Streamlit API 정리** — `use_container_width` 가 폐기 예정이라
12곳을 `width="stretch"` 로 바꿨습니다. 그대로 두면 화면에 폐기 경고가
뜰 수 있고, 시니어에게는 그게 고장으로 보입니다.

**아직 못 한 것**
- **화면 확인을 못 했습니다.** 이 세션에서 브라우저 도구 두 개가 모두 막혔습니다
  (MCP 이름 충돌). 계산·저장·우선순위는 파이썬으로 전부 확인했지만
  6·7단계 화면은 **눈으로 보지 못했습니다.** 지시서 7-3 을 못 지켰습니다
- **69곳 전체 조회는 아직 안 돌렸습니다.** 5곳 시험까지만 했습니다

**다음 세션에서 화면으로 확인할 것** (이것부터 하고 69곳을 돌리십시오)

시험용으로 5곳(1·6·25·36·51)이 `사찰기록.csv` 에 남아 있습니다. 지우지 마십시오.
5·7단계가 비어 보이지 않아야 확인이 됩니다.

| # | 어디 | 무엇을 |
|---|---|---|
| 1 | 1단계 검색창 | 안내가 "주소 또는 절 이름" 인가 |
| 2 | `봉은사` 로 검색 | 1위가 삼성동 73 이고 `(이름으로 찾음)` 이 붙는가 |
| 3 | 그 결과를 고름 | **동명이사 경고**가 뜨는가 |
| 4 | `금산사` 로 검색 | 정답이 없다는 것을 사람이 알아챌 수 있는가. **안내 문구가 부족하면 고쳐야 합니다** |
| 5 | 6단계 | 명단 69곳·조회함 5곳·남음 64곳 이 맞는가 |
| 6 | 6단계 [10곳 조회하기] | 진행 막대에 **사찰명과 남은 시간**이 보이는가. 중간에 멈춰도 이어지는가 |
| 7 | 7단계 | 방문 대상 3곳과 **판단 보류 2곳이 섞이지 않고** 따로 보이는가 |
| 8 | 4단계 | 도시가스 기준이 **순발열량 38.9MJ** 로 떠 있는가 |

화면이 멀쩡하면 그때 69곳을 돌리십시오. 64곳 x 10~20초면 **15~25분**입니다.
서버가 아프면 한 곳 3분에서 끊고 넘어가므로 최악이라도 하염없이 늘지는 않습니다.

그다음이 8번(README) 과 9번(CSV 병합) 입니다.

### 2026-09-14 (GitHub 공개 · 개인정보 정리)

**`작업지시_03.md` 개정본이 Q7 을 뒤집었습니다.** 제가 작업할 때 쓴 판은 명단
열(지정일·종단·주지스님·연락처)을 "붙이십시오" 였으나, 개정본은
**"주지스님·연락처·종단은 넣지 마십시오"**(3장 개인정보 규칙) 입니다.
개정본을 따랐습니다. `작업지시_완료_03_20260914.md` 도 개정본으로 갈았습니다.

- `store.COLUMNS` 37 -> **33열**. 명단에서 가져오는 것은 번호·사찰명뿐
- `batch.py` 가 그 네 열을 **아예 읽지 않습니다**. `Target` 에서도 뺐습니다
- 이미 쌓인 `사찰기록.csv` **36곳**에서도 제거
- `녹색사찰_명단.csv` 를 `.gitignore` 로 (지시서 6장)
- README·CLAUDE.md 의 "연락처를 빼고 보내십시오" 를 "애초에 들어오지 않는다" 로
- `test_store.py` 27갈래 · `test_bldengy.py` 12갈래 전부 통과

**파일에서 지우는 것만으로는 부족했습니다 — git 이력을 새로 시작했습니다.**

교수님이 명단 CSV 에서 개인정보를 지우셨으나, 커밋 `95aef1d` 안에 옛 판이
그대로 살아 있었습니다. **주지스님 성함 69명과 전화번호.** 그대로 올렸으면
GitHub 웹에서 커밋 하나만 눌러도 보였습니다. 한 번도 올린 적이 없어
`git checkout --orphan main` 으로 이력을 새로 냈습니다. 옛 6개 커밋은 버렸고
(내용은 이 12장에 글로 남아 있습니다), `master` 가지도 지웠습니다.

**앞으로 지킬 것** — 개인정보가 든 파일은 **커밋하기 전에 `.gitignore` 부터**
넣으십시오. 한 번 커밋되면 파일을 고쳐도 이력에 남고, 올린 뒤에는 되돌릴 수
없습니다.

- 올린 곳: https://github.com/KCS21/temple-netzero (가지 `main`, 커밋 하나)
- 올리지 않는 것: `키.txt` · `녹색사찰_명단.csv` · `사찰기록.csv` ·
  streamlit 이 만든 `.agents/` `.claude/skills/` 바로가기(남의 컴퓨터에선 깨짐)
- 올리기 전 확인 — 전화번호 0건, 60자 넘는 인증키 꼴 0건

**웹 배포는 아직입니다.** Streamlit Community Cloud 에 붙일 때 비밀 보관함에
`도로명주소` `공공데이터포털` 두 키와 `share_mode = true` 를 넣어야 합니다.
`share_mode` 가 없으면 5·6·7단계가 그대로 보입니다(7장).
