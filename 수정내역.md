# 수정 내역

2026. 9. 12.

`test_key.py` 출력문 **6곳**을 고쳤습니다. 계산·조회 로직은 한 줄도 건드리지 않았습니다.

---

## 무엇을 고쳤나

한글 윈도우 명령 창은 문자를 `cp949`라는 방식으로 찍습니다. 그런데 원본에 쓰인
긴 줄표 `—` 와 체크표 `✔` 는 cp949 안에 없는 글자입니다. 그래서 **결과를 파일로
저장하려고 하면** 조회는 다 끝내놓고 마지막 찍는 단계에서 멈춰 버립니다.

cp949 안에 있는 글자로 바꿨습니다.

| 줄 | 전 | 후 |
|---|---|---|
| 26 | `주소 검색 — "..."` | `주소 검색 - "..."` |
| 31 | `실패 — {e}` | `실패 - {e}` |
| 43 | `✔ 도로명주소 승인키 정상` | `[OK] 도로명주소 승인키 정상` |
| 48 | `건축물대장 — {주소}` | `건축물대장 - {주소}` |
| 54 | `실패 — {e}` | `실패 - {e}` |
| 65 | `✔ 공공데이터포털 인증키 정상` | `[OK] 공공데이터포털 인증키 정상` |

바꾼 것은 화면에 찍히는 글자뿐입니다. 주소를 찾고 건물을 조회하고 배출량을
계산하는 부분은 그대로입니다.

### 다른 파일에도 `—` 가 있지만 그대로 두었습니다

`juso.py` `portal.py` `bldrgst.py` `bldengy.py` `emissions.py` 에도 `—` 가 있는데,
전부 사람이 읽는 설명글(주석)에만 있고 화면에 찍히지 않습니다. 실행에 영향이 없어
건드리지 않았습니다.

---

## 정확히 말씀드릴 것

**이 수정이 없어도 명령 창에서 직접 `python test_key.py` 를 치시면 원래 잘
돌아갔습니다.** 파이썬은 명령 창에 바로 찍을 때와 파일로 넘길 때 서로 다른 방식을
쓰는데, 문제가 생기는 쪽은 파일로 넘길 때뿐입니다.

그러니까 이번 수정으로 새로 되는 일은 이것입니다.

```
python test_key.py > 결과.txt
```

결과를 파일로 남겨 두었다가 나중에 보시거나 누구에게 보내실 때, 전에는 파일이
비거나 오류 문구만 남았는데 이제 제대로 저장됩니다.

---

## 고친 뒤 확인한 것

| 해본 것 | 결과 |
|---|---|
| `python test_key.py` | 정상 |
| `python test_key.py > 결과.txt` | 정상 (전에 멈추던 경우) |
| `python test_key.py "봉은사로 531"` | 정상 |
| 일곱 파일 전체 문법 검사 | 통과 |

봉은사로 해본 결과입니다.

```
26동 / 연면적 27,271.1 m² / 전통 21동 · 신식 5동
```

목조에 기와·슬레이트를 얹은 전각 21동이 모두 「전통」으로 잡혔습니다. 분류 규칙이
실제 사찰에서도 제대로 듣는 것을 확인했습니다.

---

## 남은 일 두 가지

### 1. 실행할 파이썬을 골라 쓰셔야 합니다

지금 컴퓨터의 기본 파이썬은 **3.9.7** 인데, 화면 프로그램(Streamlit)이 하필 이
버전 하나만 콕 집어 제외합니다. 그래서 README 대로 `pip install -r requirements.txt`
를 하시면 이런 말이 나옵니다.

```
ERROR: No matching distribution found for streamlit>=1.30
```

**따로 설치하실 것 없습니다.** 컴퓨터에 이미 파이썬 3.13 과 Streamlit 이 깔려
있습니다. 아래를 그대로 쓰시면 됩니다.

```
"C:\ProgramData\anaconda3\python.exe" -m streamlit run app.py
```

`test_key.py` 는 기본 파이썬으로도 잘 돌아갑니다.

### 2. 인증키를 지우고 나눠 주십시오

`app.py` 와 `test_key.py` 맨 윗부분에 승인키가 그대로 적혀 있습니다. 모임에
보내실 때는 따옴표 안을 비우고, 각자 받은 키를 넣게 하시는 편이 안전합니다.

---

## 되돌리시려면

고치기 전 원본을 아래에 남겨 두었습니다.

```
C:\Users\kcs21\AppData\Local\Temp\claude\C--DATA-00-01-------05------------003-----jisu\91ce08b7-8d18-45a5-91d0-9749acba09b9\scratchpad\test_key.py.bak
```

임시 폴더라 언젠가 지워집니다. 계속 두실 것이면 다른 곳으로 옮겨 두십시오.
