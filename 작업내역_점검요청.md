# 클로드코드 작업 내역 — 점검 요청

사찰 넷제로 진단 도구 · 불교탄소중립실천단 지수화풍
작업일 2026-09-12 ~ 09-13

이 문서는 **검토를 받기 위한 것**입니다. 이 파일만 읽어도 판단할 수 있도록
근거 숫자와 판단 이유, 그리고 확실하지 않은 부분을 함께 적었습니다.
프로젝트 배경 전체는 같은 폴더의 `CLAUDE.md` 에 있습니다.

**인증키는 이 문서에 넣지 않았습니다.** `app.py` 와 `test_key.py` 에 평문으로
들어 있으며, 코드에서 분리하는 일이 미완 과제로 남아 있습니다.

---

## 0. 이 도구가 무엇인가 (30초 요약)

- 사찰 주소를 넣으면 **공개 API 세 개**(도로명주소 · 건축물대장 · 건물에너지)로
  건물 정보와 월별 전기·가스를 가져와 **개략 배출량**을 내는 Streamlit 화면
- 목적은 정확한 배출량이 아니라 **"방문할 가치가 있는가"** 판단
- 쓰는 사람은 모임의 **시니어 회원**. 명령어 없이 배치 파일 더블클릭으로 실행
- 올해 목표는 50개 사찰 로드맵

---

## 1. 환경 — 컴퓨터에 설치한 것

**설치했습니다.** 되돌리려면 아래 pip 패키지를 지우면 됩니다.

| 대상 | 내용 |
|---|---|
| Python 3.14.6 (`AppData/Local/Python/pythoncore-3.14-64`) | 이미 있었음. 여기에 패키지 설치 |
| 설치한 것 | streamlit 1.63.0, requests 2.34.2, pandas 3.0.5, numpy 2.5.3, pyarrow 25.0.1 등 |
| 건드리지 않은 것 | Anaconda 두 개 (3.9.7, 3.13.5) |

**왜 필요했나** — 기본 `python` 이 Anaconda **3.9.7** 인데, streamlit 이 이 버전
하나만 콕 집어 제외합니다(`Requires-Python >=3.9, !=3.9.7`). README 대로
`pip install -r requirements.txt` 를 하면 실패합니다.

---

## 2. 코드 변경 전체 목록

### 2-1. `test_key.py` — 출력 문자 6곳

`—`(긴 줄표) 4곳 → `-`, `✔` 2곳 → `[OK]`. 로직은 손대지 않았습니다.

cp949 에 없는 문자라, **결과를 파일로 저장하면** 마지막 찍는 단계에서
`UnicodeEncodeError` 로 멈췄습니다.

> **정정** — 처음에 저는 "명령창에서 그냥 실행해도 죽는다"고 보고했는데 **틀렸습니다.**
> 파이썬은 콘솔에 직접 찍을 때와 파일로 넘길 때 인코딩이 다릅니다. 실제로 죽는 건
> 파일로 넘길 때뿐이고, 명령창 직접 실행은 원래 정상이었습니다.

### 2-2. `키확인.bat` `화면열기.bat` — 파이썬 선택 버그

두 Anaconda 가 다 있으면, `if exist` 검사가 순서대로 덮어써서 **뒤쪽(3.9.7)이
이깁니다.** 게다가 그 3.9.7 의 streamlit 은 깨져 있어(`altair.vegalite.v4` 없음)
`화면열기.bat` 은 **더블클릭해도 안 열렸습니다.** `run.py` 가 "설치되어 있지
않습니다" 며 pip 명령을 안내하지만, 3.9.7 엔 설치 가능한 streamlit 이 없어
빠져나올 수 없는 상태였습니다.

`if not defined` 가드를 넣어 **먼저 찾은 것이 이기도록** 바꾸고 3.14 를 1순위로
올렸습니다. 한글은 넣지 않았습니다(CLAUDE.md 7장 규칙).
cmd 로 실제 실행해 `파이썬: 3.14.6` 이 나오는 것과 끝까지 도는 것을 확인했습니다.

### 2-3. `app.py` — 월별 그래프 x축 정렬

```
(전) f"{int(k[4:])}월"   ->  라벨 "1월".."12월"   ->  문자열 정렬로 10,11,12,1,2...
(후) f"{k[4:]}월"        ->  라벨 "01월".."12월"  ->  01~12 정상
```

난방 부담을 보여주는 그래프인데 계절 흐름이 끊겨 보였습니다.

### 2-4. `app.py` — 「산 지번만 찾기」 단추 (9줄 추가)

`산내면 입석리` 로 찾으면 **148건 30쪽**인데 산 지번은 **19쪽에 단 1건**입니다.
시니어가 `더 보기` 를 18번 눌러야 도달합니다(2장 "실패는 곧 중단" 위배).

목록에 `(산)` 이 **하나도 없을 때만** 단추가 뜨고, 누르면 검색어 끝에 `산` 을 붙여
다시 찾습니다. 화면이 저 혼자 움직이지 않습니다(2장).

실측: `산내면 입석리` 148건 30쪽 → 단추 누르면 **3건 1쪽, 산 지번이 맨 위**.

### 2-5. `bldengy.py` — 폴백 버그 (가장 중요)

건물에너지 API 는 요청에 대지구분(`platGbCd`)을 넣을 수 없습니다.
`162`(대지)와 `산162`(산)는 **둘 다 `bun=0162`** 로 변환되므로 응답에 섞여 올 수 있고,
응답의 `platGbCd` 로 걸러야 합니다.

```python
# 전
picked = [r for r in rows if r.platGbCd == str(plat_gb)]
rows = picked or rows      # <- 걸러서 비면 남의 건물을 그대로 씀

# 후
tagged = [r for r in rows if str(r.raw.get("platGbCd", "")).strip() != ""]
if tagged:
    rows = [r for r in tagged if r.platGbCd == str(plat_gb)]
# 응답이 대지구분을 아예 안 밝혔을 때만 손대지 않음
```

**무엇이 문제였나** — 산 지번 사찰을 조회했는데 그 사찰엔 자료가 없고 **같은 번지
대지 건물에만** 자료가 있으면, 남의 전기가 사찰 것으로 합산되었습니다.
숫자가 조금 틀리는 정도가 아니라 **"자료 없음 → 방문 필요"가 "신뢰도 높음"으로
뒤집혔습니다.** 화면에 아무 표시도 없었습니다.

### 2-6. `test_bldengy.py` — 신설

프로젝트 첫 테스트 파일입니다. `test_key.py` 방식에 맞췄습니다
(한 줄 실행, `[OK]`/`[FAIL]`, cp949 안전).
**인터넷도 인증키도 쓰지 않습니다** — 키 사본을 늘리지 않으려고 일부러 뺐고,
가짜 응답을 넣어 필터만 확인합니다.

아홉 갈래 전부 통과. **옛 코드에 대고 돌리면 문제의 두 갈래만 실패**하므로
헛도는 테스트가 아닙니다.

### 2-7. `app.py` — 지번 기준임을 밝히는 안내 3곳

전에는 숫자만 있고 그것이 **어느 지번 것인지 한 줄도 없었습니다.**

| 위치 | 내용 |
|---|---|
| 조회 단추 **위** | 지번 한 곳 기준이며, 고지서 주소가 가장 정확하다는 안내 |
| 결과 **바로 아래** | "이 값은 OO 지번에 잡힌 것만 더한 것입니다. 사찰 전체가 아닐 수 있습니다" |
| 자료 없을 때 | "이 지번에 없을 뿐, 다른 지번으로 잡혀 있을 수 있습니다" + 다음 행동 |

### 2-8. 문서

- `CLAUDE.md` — 4·7·8·9·10·12장 갱신 (검증 결과, 새 규칙, 면적 표, 작업 기록)
- `수정내역.md` — 첫날 `test_key.py` 수정 경위

---

## 3. 검증 결과 (실측)

### 3-1. 계산 엔진 — 보고서 재현

금산사 전력 **530.59 t**, 봉은사 가스 **536.26 t**. 소수점 둘째 자리까지 일치.

### 3-2. 건물에너지 — 보고서 대조

| 사찰 | 조회값 | 보고서 | 차이 |
|---|---:|---:|---:|
| 봉은사 | 2,316,510 (2023) | 2,310,661 | **+0.3%** |
| 수국사 | 211,844 (2022) | 208,412 | **+1.6%** |
| 금산사 | 1,037,930 (2024) | 1,117,733 | **-7.1%** |
| 실상사 | 348,369 (2024) | 352,170 | **-1.1%** |

금산사·실상사는 2022년 자료가 **0**, 2023년도 일부뿐입니다. 2024년으로 봐야 합니다.

### 3-3. 산 지번 (`platGbCd=1`) — 화면 전 구간 통과

`대자동 산162`(법문사 사찰림)로 확인. (산) 표시, 경고, 건축물대장 2동 591 m²,
**사찰 유형 「산중」 자동 전환**, 24회 호출 완주. 용도지역 자연녹지·개발제한구역.

**실상사는 산 지번이 아니었습니다.** `입석리 50-1` 대지입니다.
CLAUDE.md 가 실상사를 산 지번 검증 대상으로 적어 두었으나 전제가 달랐습니다.

### 3-4. 면적 — 네 사찰 × 세 기준

| 사찰 | 동 | 동별합계 | 주건축물만 | 총괄표제부 | 보고서 |
|---|---:|---:|---:|---:|---:|
| 봉은사 | 26 | 27,271.1 | 27,271.1 | 27,271.1 | 26,786.0 |
| 수국사 | 4 | 2,658.3 | 2,606.1 | 2,606.1 | 2,985.6 |
| 금산사 | 34 | 7,545.2 | 6,599.6 | **5,469.6** | **5,470.0** |
| 실상사 | 10 | 518.0 | 518.0 | 518.0 | 1,809.3 |

- **기준 선택은 별로 중요하지 않습니다.** 봉은사·실상사는 세 기준이 완전히 같습니다
- **진짜 문제는 대장 누락**입니다. 수국사 -11%, 실상사 **-71.4%** 는 어느 기준에서나 같음
- **금산사만 총괄표제부와 보고서가 소수점까지 일치.** 보고서 연면적도 출처가
  섞여 있는 것으로 의심됩니다 (확증 아님)

---

## 4. 작업 중 나온 도메인 판단 (교수님)

**전기요금은 전각마다 따로 부과되지 않고 특정 건축물(계량기)로 묶여 부과된다.
따라서 지번만으로 사찰 전체를 정확히 집어내는 것은 원리적으로 불가능하고,
전기가 부과되는 주소는 사람이 찾아야 한다.**

데이터가 이를 뒷받침합니다.

- 실상사 — 대장 연면적 **518 m²** 인데 연간 전기 **348,369 kWh**
- 금산사 — 2022년 **0** → 2023년 201,473 → 2024년 1,037,930 으로 널뜀

CLAUDE.md 7장에 규칙으로 기록했고, 2-7 의 화면 안내가 이 규칙의 구현입니다.

---

## 5. 아직 모르는 것 · 미해결

| 항목 | 상태 |
|---|---|
| 대지/산이 **같은 번지로 겹치는 실물 사례** | 못 찾음. 산 지번 5곳·사찰 6곳 모두 한 종류만 옴. **코드는 고쳤으나 빈도 미상** |
| **전력 배출계수 현행값** | 미정. `emissions.py` 의 `ELEC_EF` 가 딕셔너리라 값만 오면 반영 가능. 0.4747(2024 보고서)과 12% 차이 |
| **법문사 연면적·본절 주소** | 미상. 다섯 사찰 중 이 한 곳만 면적 표에 못 넣음 |
| **인증키 분리** | 미착수. `app.py`·`test_key.py` 에 평문 |
| **면적당 배출량(원단위)** | 화면에 아예 없음. 방향만 정해 둠 (누락 확인된 사찰은 내지 않기) |

---

## 6. 제가 틀렸던 것 (신뢰도 보정용)

검토하시는 분이 제 보고를 어느 정도로 믿을지 판단하실 수 있도록 적습니다.

1. **cp949 문제를 과장했습니다.** "명령창에서 바로 죽는다"고 했으나 실제로는
   파일로 저장할 때만입니다. 제 도구가 출력을 파이프로 받는 환경이라 생긴 오진입니다
2. **CLAUDE.md 에 제어문자(BEL)를 넣었습니다.** 역슬래시가 벗겨지며 생겼고,
   발견해 제거했습니다
3. **"수정본이 멈춘다"고 오진했습니다.** 실제로는 브라우저 세션이 엉킨 것이었고
   코드는 정상이었습니다. 새 탭에서 확인해 바로잡음
4. **f-string 에 이중 중괄호가 들어가** 값 대신 글자가 찍힐 뻔했습니다.
   **컴파일은 통과하므로** 화면을 안 봤으면 놓쳤을 문제입니다

→ 공통점: **컴파일이나 단위 확인만으로는 안 걸리고 화면을 봐야 걸리는 문제**가
   여럿 있었습니다. 앞으로도 실제 화면 확인을 빼지 않는 편이 좋겠습니다.

---

## 7. 점검받고 싶은 것

### 설계 판단

1. **「산 지번만 찾기」** — 검색어 끝에 `산` 을 붙이는 방식이 적절한가?
   `산내면` 처럼 지명에 이미 `산` 이 든 경우가 있고, 정확도가 보장되지도 않습니다.
   더 나은 방법이 있는지
2. **`bldengy` 폴백 수정** — 안 맞으면 **0** 을 돌려주는 것이 맞는가?
   아니면 "걸러냈음" 을 화면에 표면화해야 하는가?
3. **면적 기준** — "대장 값 그대로 + 어긋남 표시 + 원단위는 누락 없을 때만" 이
   맞는가? 기존 다섯 사찰과의 비교는 어떻게 유지하는가?
4. **커버리지 보정** (도심 0.93 / 산중 0.88) — 소계를 나눠 총량을 추정하는 지금
   방식을 유지할지, 소계만 보이고 나머지는 "미확인" 으로 둘지 (CLAUDE.md 10장 3번)

### 방향

5. **계량기 가설을 어떻게 검증하는가?** 한전 계약 정보나 고지서 없이 확인할 방법이
   있는지. 없다면 도구는 어디까지만 해야 하는지
6. **전력 배출계수** — 현행값을 쓸지, 0.4747 을 유지할지, 둘 다 두고 고르게 할지
7. **다음에 만들 것이 무엇인가?** 50개 사찰 로드맵이 목표인데, 지금 도구는
   **한 번에 한 곳**만 봅니다. 여러 곳 저장·비교, 문서 출력, 계약전력 분석 중
   무엇이 실제로 급한지
8. **인증키 분리 방식** — 설정 파일 / 환경변수 / 각자 입력 중 무엇이
   시니어 사용자에게 맞는지 (웹 배포 여부와 연결됨)

---

## 8. 현재 폴더

```
CLAUDE.md               작업 인계 문서 (배경·규칙·기록)
README.md               사용 설명
작업내역_점검요청.md     이 문서
수정내역.md             첫날 수정 경위
검증할주소.md           넣어 볼 주소와 정답

app.py                  화면 (Streamlit). 인증키 있음
juso.py                 주소 검색·파라미터 변환
portal.py               공공데이터포털 공통 호출
bldrgst.py              건축물대장
bldengy.py              월별 전기·가스
emissions.py            배출량 계산
run.py                  배치 파일이 부르는 시작 프로그램
test_key.py             인증키 확인. 인증키 있음
test_bldengy.py         대지/산 필터 확인 (인터넷·키 불필요)
키확인.bat 화면열기.bat   더블클릭 실행

requirements.txt
```

**실행** — `화면열기.bat` 더블클릭, 또는 Python 3.14 로 `-m streamlit run app.py`.

전체 컴파일 통과, `test_bldengy.py` 9/9 통과 상태입니다.
